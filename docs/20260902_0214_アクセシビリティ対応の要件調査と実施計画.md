# アクセシビリティ対応の要件調査と実施計画

作成日: 2026-09-02

## 0. この文書の位置づけ

「WCAG の最新版への適合」と「VoiceOver による操作の最適化」に向けた、要件の調査・現状分析・実施計画をまとめる。実装はこの文書の合意後に着手する。

**重要な前置き**: 本書の「適合/不適合」は、記載した対象・範囲・証拠に基づく**自己評価**である。第三者認証でも、組織としての正式な適合表明でもない。正式な表明には人間のレビュワーによる全項目の検証が別途必要（→ 6章）。

---

## 1. 適合すべき基準の整理

### 1.1 WCAG の「最新版」とは何か

| 版 | 位置づけ | 本プロジェクトでの扱い |
| :-- | :-- | :-- |
| WCAG 2.0 | 2008年勧告。JIS X 8341-3:2016 の技術的内容と一致 | 下限。JIS 経由で公共分野の実質的な要求水準 |
| WCAG 2.1 | 2018年勧告。モバイル・ロービジョン・認知の17項目を追加 | 2.2 に包含される |
| **WCAG 2.2** | **2023年10月勧告。現行の最新勧告** | **本計画の目標。レベルAA** |
| WCAG 3.0 | 草案（Working Draft）。勧告化の時期未定 | 対象外。安定していないため適合対象にしない |

**結論: 目標は WCAG 2.2 レベル AA。**

WCAG 2.2 は 2.1 に9項目を追加し、1項目（4.1.1 構文解析）を廃止した。2.1 からの追加分のうち AA は以下6項目で、いずれも本サービスに関係する:

- 2.4.11 Focus Not Obscured (Minimum) — 固定ヘッダーがフォーカス中の要素を隠さない
- 2.5.7 Dragging Movements — ドラッグ以外の手段を用意する
- 2.5.8 Target Size (Minimum) — タップ領域 24×24 CSS px 以上
- 3.2.6 Consistent Help — ヘルプの導線を一貫した位置に置く
- 3.3.7 Redundant Entry — 同じ情報を再入力させない
- 3.3.8 Accessible Authentication (Minimum) — 認知機能テストに頼らない認証

（2.4.12 Focus Not Obscured (Enhanced)、2.4.13 Focus Appearance、3.3.9 は AAA のため対象外。ただし 2.4.13 は実用上重要なので参考として扱う。）

### 1.2 日本の公共分野で追加的に求められる水準

本サービスは沼津市議会の情報を市民に届けるもので、公共性が高い。以下を併せて満たす前提で設計する。

- **JIS X 8341-3:2016**（高齢者・障害者等配慮設計指針—情報通信における機器,ソフトウェア及びサービス—第3部:ウェブコンテンツ）
  技術的内容は WCAG 2.0 と一致。**WCAG 2.2 AA を満たせば自動的に満たす**。
- **総務省「みんなの公共サイト運用ガイドライン」**
  公的機関に JIS X 8341-3:2016 の**レベル AA 準拠**を求める。準拠の表明には試験結果の公開が必要。
- **障害者差別解消法**
  民間事業者にも合理的配慮の提供が義務。運営主体が民間でも配慮義務は生じる。

**運営主体が未確定**（`docs/20260901_2220_沼津市向けブランディング対応状況.md` 参照）のため、「準拠」を公式表明するかは公開時に判断する。技術水準は表明の有無にかかわらず AA を目指す。

### 1.3 本サービス固有の考慮

議会情報を「あらゆる沼津市民が理解できる」ことが目的なので、達成基準を満たすこと自体より、以下の利用者が実際に使えるかを軸に据える。

- スクリーンリーダー利用者（VoiceOver / NVDA / TalkBack）
- ロービジョン・色覚特性のある利用者（拡大、コントラスト）
- 高齢の利用者（タップ領域、文字サイズ、専門用語）
- 認知・学習に困難のある利用者（既存の「ふつう/くわしい」難易度切替はこの層への配慮として既に有効）

---

## 2. VoiceOver 検証環境の調査結果

### 2.1 結論: iOS Simulator では VoiceOver の操作テストはできない

実地に検証した結果を記録する。

**検証手順と結果**:

```
# シミュレータで VoiceOver の設定値を有効化
xcrun simctl spawn <UDID> defaults write com.apple.Accessibility VoiceOverTouchEnabled -bool true
xcrun simctl spawn <UDID> defaults read com.apple.Accessibility
  → VoiceOverTouchEnabled = 1     # 設定値は書き込まれる

# 実際にプロセスが動いているか
xcrun simctl spawn <UDID> launchctl list | grep -i voiceover
  → -  0  com.apple.VoiceOverTouch   # PID が "-" = 起動していない
xcrun simctl spawn <UDID> ps aux | grep -i voiceover
  → 該当なし
```

画面を撮影しても VoiceOver カーソル（フォーカス枠）は現れなかった。**設定値は立つが VoiceOver 本体は起動しない**。これは Simulator の既知の制約で、読み上げ・ジェスチャ操作（スワイプでの要素送り、ダブルタップでの実行、ローター）は再現できない。

### 2.2 一方、Simulator で「できること」

同じ検証で、アクセシビリティ関連の以下は**稼働している**ことを確認した。

```
57666  0  com.apple.AccessibilityUIServer                      # 稼働
-      0  com.apple.accessibility.axAuditDaemon.deviceservice  # 監査デーモン
```

つまり **Accessibility Inspector（Xcode 同梱、`Xcode.app/Contents/Applications/` に存在を確認済み）から Simulator に接続し、アクセシビリティツリーの検査と自動監査ができる**。VoiceOver が読み上げる元データ（label / role / traits / value）はここで確認できるため、「何がどう読まれるか」の大半は Simulator で検証可能。

### 2.3 したがって検証は3層に分ける

| 層 | 手段 | 何を検証するか | 自動化 |
| :-- | :-- | :-- | :-- |
| **L1 静的** | axe-core（Playwright / vitest） | 機械判定できる違反（コントラスト、ラベル欠落、見出し順、ARIA誤用） | CI で自動 |
| **L2 ツリー** | Accessibility Inspector + iOS Simulator | 要素の名前・役割・値・traits。読み上げ内容の元データ。自動監査機能 | 半自動（GUI） |
| **L3 実操作** | **実機 iPhone + VoiceOver** | ジェスチャ操作、読み上げ順、ローター、実際の使い勝手 | 手動 |

**L3 は実機が必須**。Simulator では代替できない。macOS Safari + VoiceOver（Mac 本体の VoiceOver は動作する）も、同じ WebKit 系エンジンなので L3 の一次近似として有効。

### 2.4 環境整備で発生した問題（記録）

Simulator が起動しなかった。原因と対処を残す。

- **症状**: `xcrun simctl create` が `Device was allocated but was stuck in creation state` で失敗
- **原因**: `~/Library/Developer` が外付けSSD（`/Volumes/ExSSD`）へのシンボリックリンクで、`CoreSimulatorService` が当該ボリュームに書き込めない（`NSCocoaErrorDomain Code=513 "You don't have permission"`）。macOS の TCC によるリムーバブルボリューム保護と見られる
- **効かなかった対処**: Xcode / Simulator.app へのフルディスクアクセス付与。**実際に書き込むのは `CoreSimulatorService` という XPC デーモンで、これらの許可を継承しない**
- **有効だった対処**: デバイス保存先だけを内蔵ディスクへ退避
  ```bash
  BASE=/Volumes/ExSSD/MigratedData/Library/Developer/CoreSimulator
  mv "$BASE/Devices" "$BASE/Devices.old"      # 既存15GBは削除せず残す
  mkdir -p ~/Library/CoreSimulatorDevices
  ln -s ~/Library/CoreSimulatorDevices "$BASE/Devices"
  ```
  DerivedData(16GB) と Xcode(20GB) は外付けのまま。戻す場合はリンクを削除して `Devices.old` を `Devices` に戻す。

---

## 3. 現状分析（WCAG 2.2 AA 監査の結果）

`web/` を対象に、コードとレンダリング済み HTML を検査した。55項目中13項目を実検査、42項目は実機検証が必要なため未実施。

### 3.1 確定した不適合

| # | 達成基準 | 事象 | 該当箇所 |
| :-- | :-- | :-- | :-- |
| **A-1** | 1.4.3 コントラスト(最低限) | `--color-mirai-text-muted` (#8e8e93) が白地 **3.26:1**、本文地 **2.97:1**。要 4.5:1 | 一覧のページ番号・提出日・空状態文言など広範囲 |
| **A-2** | 1.4.3 コントラスト(最低限) | `--color-mirai-text-placeholder` (#aeaeb2) が白地 **2.21:1** | 検索欄のプレースホルダ |
| **A-3** | 1.3.1 情報及び関係性 | **トップページに `h1` が0件**。最初の見出しが `h2` から始まる | `web/src/app/(main)/page.tsx` |
| **A-4** | 1.3.1 情報及び関係性 | 会期ステータスカード（「本日は会期中」）が `span` のみで見出しを持たない | `current-council-session.tsx` |
| **A-5** | 2.4.11 フォーカスの被覆(最低限) | 「注目」チップが `#featured` へジャンプするが、遷移先に `scroll-mt-*` が無く固定ヘッダーに隠れる。同コードベースの `chat-log-section.tsx` では `scroll-mt-24` を使っており対策漏れ | `category-tabs.tsx` / `page.tsx` |
| **A-6** | 2.5.8 ターゲットのサイズ(最低限) | 「AIインタビュー受付中のみ表示」リンクが高さ約19–20px（縦パディング無し）。要 24×24px | `bills-list-page.tsx` |
| **A-7** | 4.1.2 名前・役割・値 | 上記リンクが `role="checkbox"` + `aria-checked` だが実体は `Link`。ARIA チェックボックスが要求する **Space キーでのトグルに未対応**（Enter のみ） | 同上 |
| **A-8** | 4.1.3 ステータスメッセージ | 検索候補が0件のときは `aria-live` があるが、**件数が出るとき（0→N, N→M）は無通知** | `bill-search-overlay.tsx` |

### 3.2 参考（AAA だが実用上重要）

| # | 事象 |
| :-- | :-- |
| B-1 | 2.4.13 フォーカスの外観: `Button` が `outline-none` でグローバルの outline を打ち消し、`ring-primary/40` を使用。白地 **1.82:1** / ボタン背景 **1.42:1** で、非テキスト 3:1 を下回る。**全ページ共通のボタン**でキーボードフォーカス位置が視認しづらい |

### 3.3 要確認（実機検証が必要）

| 達成基準 | 論点 |
| :-- | :-- |
| 2.1.1 キーボード操作 | 検索モーダルは Radix Dialog 依存でフォーカストラップ等は実装されているはずだが未確認。カテゴリタブの横スクロール、AIチャットも未確認 |
| 2.4.1 ブロックスキップ | `main` ランドマークは存在するが**可視のスキップリンクが無い**（検証で確認） |
| 2.5.3 名前に含まれるラベル | 検索欄に可視ラベルが無く、プレースホルダ「議案名やキーワードで探す」と `aria-label="議案を検索"` が不一致 |
| 1.4.4 / 1.4.10 拡大・リフロー | 200%拡大、320px幅での破綻有無 |
| 2.5.7 ドラッグ動作 | カテゴリチップの横スクロールにドラッグ以外の手段があるか |

### 3.4 適合を確認したもの

- 2.4.2 ページタイトル: 3画面とも一意で説明的
- 3.1.1 ページの言語: `<html lang="ja">`
- **今回の配色変更はすべて AA 適合**: `--primary`(#1b6ca8) 白地 5.59:1 / 本文地 5.09:1、`--primary-accent`(#14507c) 7.75〜8.51:1

### 3.5 その他の観察

- パンくずが `nav` に `aria-label` を持たず、`ol/li` 構造でもなく、現在地に `aria-current="page"` も無い。同一ページの他の `nav`（カテゴリ・ページ送り）はラベル付きなので、ここだけランドマークとして識別しづらい

---

## 4. 実施計画

### フェーズ1: 機械判定できる不適合の是正（優先）

小さく確実に効く。既存コンポーネントの修正のみで、設計変更を伴わない。

| 項目 | 対応 | 影響範囲 |
| :-- | :-- | :-- |
| A-1 / A-2 | `--color-mirai-text-muted` を 4.5:1 を満たす色（#6b6b70 前後）へ。`--color-mirai-text-placeholder` も同様。あるいは用途を大きい文字専用に分離 | globals.css のトークン変更。使用箇所が広いので目視確認が要る |
| A-3 / A-4 | トップページに `h1` を追加（視覚的に控えめでも可）。会期カードに見出しを付与 | 2ファイル |
| A-5 | `#featured` に `scroll-mt-*` を付与。可能ならグローバルに `scroll-padding-top` | 1〜2ファイル |
| A-6 / A-7 | 「受付中のみ表示」を本物の `input[type=checkbox]` + ラベルにする（`role` の付け替えより確実で、Space キーもタップ領域も同時に解決） | 1ファイル。Client Component 化が必要 |
| A-8 | 件数表示を含むコンテナに `aria-live="polite"` を常時付与 | 1ファイル |
| B-1 | `Button` の `outline-none` を外すか、リングを 3:1 以上にする | 全ページに影響。要目視確認 |
| 3.5 | パンくずに `aria-label="パンくずリスト"`、`ol/li` 化、最終項目に `aria-current="page"` | 1ファイル |
| 2.4.1 | スキップリンクを追加（`main` へ） | レイアウト1ファイル |

### フェーズ2: 検証の自動化（回帰させない仕組み）

フェーズ1の是正が壊れないようにする。**先に入れておくとフェーズ1の検証にも使える**ので、実際は1と並行で着手したい。

- `axe-core` を導入し、主要画面（`/`, `/bills`, `/bills/{id}`, インタビューLP、レポート）に対する自動チェックを CI に追加
- コントラストは**トークン単位のユニットテスト**にする。globals.css の値を読んで比を計算し、閾値を割ったら落とす。色を変えたときに気づける（今回の配色変更のような場面で効く）
- ページ単位の axe 実行は Playwright を新規導入するか、既存の vitest + jsdom で完結させるかを比較して決める（jsdom は CSS を評価しないためコントラスト検査に向かない点に注意）

### フェーズ3: スクリーンリーダーでの実地検証

自動チェックでは「読み上げて意味が通るか」は判定できない。ここは人が確認する。

1. **L2（Simulator + Accessibility Inspector）**: 主要画面のツリーを検査し、名前・役割・値が意図どおりか確認。自動監査機能も併用
2. **L3（実機 iPhone + VoiceOver）**: 以下のシナリオを通しで操作
   - トップから議案を探して詳細を読む
   - 一覧の絞り込み（ステータス・カテゴリ・受付中のみ）とページ送り
   - AIインタビューの開始から回答、公開同意まで
   - AIチャットでの質問
3. **macOS Safari + VoiceOver**: 実機が用意できるまでの一次近似として使う

検証項目は「読み上げ順が視覚順と一致するか」「操作後に何が起きたか伝わるか」「議案番号・和暦・専門用語が正しく読まれるか」を重視する。**議会用語の読み上げ**（例:「議第63号」「専決処分」「認第31号」）は本サービス固有の論点で、必要なら `aria-label` で読みを補う。

### フェーズ4: 継続的な体制

- 是正内容と検証結果を記録し、公開時に「アクセシビリティ方針」ページとして出せる形にする（みんなの公共サイト運用ガイドラインが求める試験結果の公開に対応）
- 新規 UI 追加時のチェックリストを `AGENTS.md` に追加

---

## 5. 未決事項（判断が必要）

| # | 論点 | 選択肢 |
| :-- | :-- | :-- |
| 1 | **実機 iPhone を検証に使えるか** | 使える → フェーズ3の L3 を計画どおり実施 / 使えない → L2 + macOS VoiceOver までとし、L3 は公開前に別途確保 |
| 2 | **「AA 準拠」を公式に表明するか** | 表明する → 全55項目の試験と結果公開が必要（工数大）/ 表明しない → 技術水準は AA を目指しつつ、方針ページでは「配慮事項」として記載 |
| 3 | **自動チェックの実行基盤** | Playwright を新規導入（実ブラウザで正確、CI 時間増）/ 既存 vitest で完結（軽いがコントラスト検査に限界） |
| 4 | **フェーズ1の実施単位** | 1つのPRでまとめる / 影響範囲ごとに分割（コントラスト変更は全画面に及ぶため分けたい） |

---

## 6. 本文書の限界

- 監査は 55項目中 13項目の実検査に基づく。**42項目は未検証**であり、「不適合が8件」は「見つかった不適合が8件」であって上限ではない
- 実機・支援技術での操作検証は未実施。キーボード操作、リフロー、拡大表示の判定は保留している
- 正式な WCAG / JIS 適合表明には、人間のレビュワーによる全項目の検証記録が別途必要
